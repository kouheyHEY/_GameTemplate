# リリース前TODOリスト

## プロジェクトステータス

- **バージョン**: v0.9 (機能実装完了)
- **ターゲット**: Windows (Electron), Web (Phaser)

## 残タスク一覧

### 1. サウンド実装 (Sound Implementation)

- [ ] `SoundManager` の作成 (音量管理, 再生キュー)
- [ ] 効果音アセットの収集・作成 (typing, enter, error, success, cursor)
- [ ] `TerminalScene` と `SystemMenu` への効果音組み込み
- [ ] BGM/アンビエントノイズの実装 (TitleScene, In-Game)
- [ ] 音量調整 (Options画面などへの追加は任意、今回はデフォルトバランス調整で良い)

### 2. レベルデザイン最終調整 (Content Polish)

- [ ] `ep_abyssal_cipher` の難易度再チェック (ヒントが適切か)
- [ ] `ep_challenge` の解法確認と、エンディングテキストの推敲
- [ ] テキストの誤字脱字チェック (全シナリオ通しプレイ推奨)
- [ ] `mail_list.json` の送信元・件名の統一感チェック

### 3. デバッグ・クリーンアップ (Cleanup)

- [ ] `ep_dev_secret_notes.json` の削除または無効化 (本番ビルドには含めない)
- [ ] `mail_dev_notes` エントリの削除またはコメントアウト
- [ ] コンソールログ (`console.log`) の削除または製品版抑制 (`config.debug = false`)
- [ ] 未使用アセットの削除

### 4. ビルドプロセス (Build & Distribute)

- [ ] `package.json` のバージョン更新 (v1.0.0へ)
- [ ] Windows用アイコン (`.ico`) の設定・ビルド確認
- [ ] `npm run build:desktop` (Electronビルド) の実行と成果物確認
- [ ] ビルド済みアプリの別PCでの動作確認 (依存ライブラリの欠落チェック)

### 5. メタデータ (Meta)

- [ ] `README.md` の作成 (操作方法、ストーリー概要、クレジット表記)
- [ ] ライセンス表記の確認 (使用したフォント、効果音、画像素材の権利表記)
- [ ] スクリーンショット撮影 (ストア掲載用、共有用)

### 6. 標準化・資産化 (Standardization)

- [ ] ドキュメント構成の反映: 今回のプロジェクトで改善された `docs` フォルダの構成やルールを、開発標準テンプレートへ反映する
- [ ] ドキュメントのリポジトリ化: `docs` フォルダの内容を独立したGitリポジトリとして切り出し、他プロジェクトで再利用可能な状態にする

---

## 優先順位

1. **サウンド実装**: 体験の質を大きく左右するため最優先。
2. **クリーンアップ**: デバッグ機能の消し忘れは品質に関わる。
3. **ビルド確認**: そもそも動かないと意味がないため、早めに確認する。
